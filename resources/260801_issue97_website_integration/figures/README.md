# Figures (screenshots)

Drop PNGs here with the filenames below. Markdown in the theme READMEs already points at these paths.

Capture from a local petadex.io run (API `:3001`, site `:8000`) when you can.

| File | What to capture | Theme |
|------|-----------------|-------|
| `cath-inline-molstar.png` | CATH domain page with in-page Mol* + PDB picker if multi-PDB | CATH |
| `sequence-features.png` | Sequence Features tab with domain / catalytic tracks | Sequence / 3D |
| `structure-callouts.png` | Structure tab with residue highlight + callout (e.g. P52956 / B0FLR6) | Sequence / 3D |
| `signalp-orf.png` | ORF page showing SignalP prediction (e.g. orf `294247546`) | Sequence / 3D |
| `search-view-phylogeny.png` | Search results family summary with **View phylogeny** | Phylo |
| `tree-highlight-search.png` | `/tree/:id` with search-hit tips highlighted + banner | Phylo |
| `tree-nav-path-neighbors.png` | Focused tip: path-to-root + neighbors / dim far tips | Phylo |
| `folding-viewer-orf.png` | Folding Viewer on ORF `4981589` (Alex `test2`) | Alex folds |
| `biosample-bacdive-means.png` | BioSample page with BacDive means panel (CSV #3 loaded) | SRA / BacDive |

After adding a file, you can reference it from a theme README like:

```md
![CATH inline Mol*](../figures/cath-inline-molstar.png)
```

Until the PNGs exist, the checklist above is the source of truth for what still needs capturing.
